$(document).ready(function(){

  $('#contact-text').bigtext();

  Shadowbox.init({
      handleOversize: "resize",
      initialWidth: 600,
      initialHeight: 400,
      overlayOpacity: .8,
      resizeDuration: .3,
      animSequence: "sync"
  });
});
